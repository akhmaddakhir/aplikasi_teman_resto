import 'package:flutter/material.dart';

class AppColors {
  // Primary orange (dipakai di tombol, aksen utama)
  static const Color primary = Color(0xFFFF4F0F);

  // Background abu muda
  static const Color inputFill = Color(0xFFF0F0F0);
  static const Color cardBg = Color(0xFFF5F5F5);

  // Warna teks
  static const Color textDark = Colors.black;
  static const Color textGrey = Colors.grey;

  // Warna status
  static const Color reserved = Color(0xFFFF4F0F);
  static const Color available = Color(0xFFEDEDED);
  static const Color selected = Color(0xFF43EA3B);
}
